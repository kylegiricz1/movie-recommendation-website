version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "5200:5200"
    volumes:
      - ./backend/uploads:/app/uploads

  frontend:
    build: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
